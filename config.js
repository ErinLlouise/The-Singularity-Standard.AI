// Deadly Serious — frontend config.
// Paste your Cloudflare Worker URL below after running `wrangler deploy`
// in the worker/ directory. See worker/README.md for one-time setup.
//
// Leaving workerUrl as an empty string is fine — the AI-driven sections
// will show a "needs configuration" state instead of failing.

window.DEADLY_SERIOUS_CONFIG = {
  workerUrl: "",
};
